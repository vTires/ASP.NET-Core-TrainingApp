@if (Model.Count == 0)
{
    <p>No employees have been added yet.</p>
}
else
{
    @foreach (var employee in Model)
    {
        <li id="expired-employee-@employee.Id">@employee.Id - @employee.Name - @employee.Position - @employee.IsAgreement - @employee.Date</li>
    }
}
